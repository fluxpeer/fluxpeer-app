<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive launcher icon (API 26+): dark-indigo brand background + the mesh
     mark foreground, masked to the device's shape (no hard square). Matches the
     admin-lite header mark + the splash. -->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@color/fluxpeer_splash" />
    <foreground android:drawable="@mipmap/ic_launcher_foreground" />
</adaptive-icon>
